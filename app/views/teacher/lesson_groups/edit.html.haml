.mb16 講座編集
.flex
  .lesson_group_forms
    = render 'teacher/lesson_groups/form', lesson_group: @lesson_group
    %hr.mb24
      .mb16 授業追加
      = render 'teacher/lessons/form', lesson: @lesson
  .lessons
    - @lessons&.where&.not(id: nil)&.each do |lesson|
      .flex.mb24
        .mr32= "#{lesson.name} (#{lesson.questions.count}題)"
        .mr24= link_to '課題作成・追加', new_teacher_lesson_question_path(lesson)
        .mr24= link_to '詳細', teacher_lesson_path(lesson)
      %hr
