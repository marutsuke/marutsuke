講座一覧
%hr
= link_to '講座新規作成', new_teacher_lesson_group_path
%hr.mb8
- if @lesson_groups.size == 0
  現在講座は登録されていません。
-else
  - @lesson_groups.each do |lesson_group|
    %hr
    .lesson_group= lesson_group.name
