.teacher_header
  = link_to teacher_path do
    .teacher_header__logo
      = image_tag ('marutsuke_logo_b.png'), art: 'marutsuke'
  = link_to teacher_lessons_path do
    .teacher_header__element 提出課題管理
  = link_to teacher_lesson_groups_path do
    .teacher_header__element 講座作成
  = link_to teacher_users_path do
    .teacher_header__element 生徒管理
  = link_to teacher_teachers_path do
    .teacher_header__element 教員管理
  = link_to edit_teacher_school_path(current_teacher_school) do
    .teacher_header__element 学校情報
  = link_to teacher_school_buildings_path do
    .teacher_header__element 校舎管理
  .teacher_name= "#{ current_teacher.name }先生(所属: #{current_teacher.main_school_building&.name || 'なし'})"

  = link_to teacher_logout_path, method: :delete do
    .teacher_header__element ログアウト
  .teacher_header__space
