$("<%="#js-lessons_table"%>")
  .html("<%= j render(
    partial: 'teacher/answer_checks/lessons_table',
    locals: {
      lesson_group: @lesson_group,
      lessons: @lessons
    }) %>");

$("<%="#js-lesson_group--#{@lesson_group.id}"%>")
  .html('授業一覧');
